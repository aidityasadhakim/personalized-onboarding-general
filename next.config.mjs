/** @type {import('next').NextConfig} */
const nextConfig = {
  // The dev badge sits on top of the flow, and this gets shown on a screen share.
  devIndicators: false,
};

export default nextConfig;
