# OpenRouter (used through the Vercel AI SDK)
OPENROUTER_API_KEY=
# Any OpenRouter model id. When no ":variant" is given, ":nitro" is appended so the
# request routes to the fastest provider instead of the cheapest.
OPENROUTER_MODEL=deepseek/deepseek-v4-flash
